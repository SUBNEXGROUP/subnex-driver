{
  "name": "subnex-driver",
  "private": true,
  "description": "SUBNEX: пульт (admin.html) и приложение водителя (index.html). Проверки: npm test.",
  "scripts": {
    "check": "node --check ops-dispatch-core.js && node --check ops-dispatch.js && node --check ops-chat.js",
    "test:core": "node _test/core.test.mjs",
    "test:gs": "node _test/gs.test.mjs",
    "test:smoke": "node _test/smoke.mjs",
    "test": "npm run check && npm run test:core && npm run test:gs && npm run test:smoke"
  },
  "devDependencies": {
    "playwright": "^1.56.0"
  }
}
